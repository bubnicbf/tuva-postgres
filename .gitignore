# env & creds
.env
*.env
profiles.yml

# python
__pycache__/
*.pyc
.venv/
*.egg-info/

# dbt artifacts (regenerated by `dbt deps`/`dbt build`, never committed --
# see dbt_project.yml's clean-targets and README.md)
dbt_packages/
target/
logs/

# raw extraction/snapshot output (immutable, operator-owned; never
# committed -- see src/tuva_ingest/extract.py and RAW_DATA_DIR)
data/
tmp/

# macOS
.DS_Store
